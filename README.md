
  # EduCampus SRS (Copy)

  This is a code bundle for EduCampus SRS (Copy). The original project is available at https://www.figma.com/design/EqrN4ehXoEu3hjw0HrM4ix/EduCampus-SRS--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  